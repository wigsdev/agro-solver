/**
 * Agro-Solver Frontend Configuration
 * Centralized configuration to avoid hardcoding values in logic files.
 */
const CONFIG = {
    // TODO: Change this URL when deploying to production
    API_URL: "http://127.0.0.1:8000"
};
